# errcheck exclusions for acceptable unchecked errors

# Defer cleanup operations - all deferred calls are acceptable to ignore
defer

# Close operations - cleanup code where errors are not actionable
(*.Close)

# JSON unmarshaling - errors handled at higher level or acceptable to ignore
encoding/json.Unmarshal

# Migration tracker operations in error handling paths
(github.com/nicolasbonnici/gorest/migrations.MigrationTracker).RecordFailedMigration
