# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mydb?sslmode=disable
DB_PASSWORD=postgres

# JWT Configuration
# Generate a strong random secret for production using: openssl rand -base64 32
JWT_SECRET='1Xpgw302pYUP98RQ+nBWs6ED2Ht0EMwYKd3pZ3+c7lE='
# JWT token expiration time in seconds (default: 900 = 15 minutes)
JWT_TTL=900

# Pagination Configuration
PAGINATION_LIMIT=10
PAGINATION_MAX_LIMIT=1000

# Server Configuration
PORT=3000
